import Setup from "@/views/Setup";

export const metadata = { title: "Configuración inicial — CRM Nexo" };

export default function SetupPage() {
  return <Setup />;
}

export const dynamic = 'force-dynamic';
