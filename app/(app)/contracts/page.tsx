import Contracts from "@/views/Contracts";

export const metadata = { title: "Contratos — CRM Nexo" };

export default function Page() {
  return <Contracts />;
}

export const dynamic = 'force-dynamic';
