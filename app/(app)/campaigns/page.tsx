import Campaigns from "@/views/Campaigns";

export const metadata = { title: "Campañas — CRM Nexo" };

export default function Page() {
  return <Campaigns />;
}

export const dynamic = 'force-dynamic';
