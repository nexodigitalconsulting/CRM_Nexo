import Calendar from "@/views/Calendar";

export const metadata = { title: "Calendario — CRM Nexo" };

export default function Page() {
  return <Calendar />;
}

export const dynamic = 'force-dynamic';
