import Settings from "@/views/Settings";

export const metadata = { title: "Configuración — CRM Nexo" };

export default function Page() {
  return <Settings />;
}

export const dynamic = 'force-dynamic';
